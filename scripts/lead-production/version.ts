// Bump MATCHING_RULE_VERSION whenever match-customers.ts, screen-large-groups.ts, or
// rejection-levels.ts's rule logic changes — it is written into every processing-summary.json
// so historical output files stay attributable to the exact rule set that produced them.
export const MATCHING_RULE_VERSION = "1.0.0";
