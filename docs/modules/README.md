# Module Docs

Active integration/module docs go in this folder.

Unused templates live in `_optional/`. Do not create active module docs just because the folder looks lonely. That is how documentation turns into compost.
