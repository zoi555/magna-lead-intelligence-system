# Pipeline / Map / Export Refinement (Phases 9–11)

## Phase 9 — Pipeline Monitor (`/pipeline-runs`)
Kept both tabs (Operational Status + Flow Network) and the animation. Added server-side panels:
- **Latest run summary:** run id, status, started/completed, final leads, exportable, manual-review,
  mapped/total, warnings, errors, failed stage, unmapped (missing coords).
- **Source readiness strip** per run (from `source-registry`).
- **Local export files:** availability + size for `first-fsa-leads.csv/json` and
  `first-fsa-telesales-safe.csv` — "generated locally" (no fake browser download).
- **Stage error summary:** top warnings by code, errors by code, failed stage.
- Honest controls unchanged: Pause/Resume/Retry are UI concepts (CLI/manual run only).
Helper: `src/lib/pipeline/run-report.ts` (`summariseRun`, `errorBreakdown`, `exportFilesStatus`, `countWarning`).

## Phase 10 — Map (`/coverage-map`, `MapEngine`)
No rebuild, no colour change, no MapLibre. Practical polish:
- **Default road mode = Primary A roads** ("All A roads" still available, marked "⚠ heavy").
- **Counts:** total leads, mapped, unmapped, roads-shown, active granularity + road mode, plus a
  clear **"Some leads are not shown because coordinates are missing (N)"** note.
- **Legend overlay:** coverage / gap / expansion / selected / motorway / A road + grade A–D dots + presence note.
- **Selected territory panel:** lead count, grade breakdown, top categories, platform-unknown count,
  missing-phone count, export-eligible / manual-review split.
- **Selected lead panel:** business, postcode, category, grade, phone-present, platform status, export
  status, warning badges — **no numeric score / reasons** shown (grade only).

## Phase 11 — Export Review + Telesales safety
- **`/export-review`:** honest counts (final, exportable, manual-review, held, excluded, missing
  coords), source-warning counts (missing phone, platform unknown, Companies House not configured,
  Google disabled, delivery manual), local export file paths, and an explicit **gate LOCKED + reason**
  with "Approval workflow not yet implemented — local exports generated by pipeline." No fake approval.
- **`/telesales`:** consumes **only** the safe-view helper.
- **`src/lib/pipeline/telesales-safe-view.ts`:** `toTelesalesSafeLead`, `toTelesalesSafeQueue`,
  `assertNoRestrictedTelesalesFields`. Safe fields only: business_name, postcode, phone, category,
  trigger_reason, assigned_rep, worked_status, source_warning.
- **`scripts/test-telesales-safe-view.ts`** (`npm run test:telesales-safe`) — fails if any restricted
  field appears. **Passes: 466 safe rows, no leaks.**

## Real vs placeholder
- **Real:** all monitor/export counts, error breakdowns, map counts and lead points, telesales safe queue.
- **Placeholder:** browser download (paths shown instead), CRM approval workflow (not implemented,
  stated honestly), enrichment (CH/Google/delivery still disabled → warnings only).

## Export / telesales safety rules
- Telesales never sees internal score, score reasons, matching internals, confidence, Companies House
  financials, enrichment internals, or customer-match details (enforced by the safe-view + assert + test).
- Delivery presence is never a blocker; unknown → warning/manual review.

## Remaining blockers
Enrichment disabled (phone/CH/Google) → warnings on most leads. CRM export gated (ISS-0003), approval
workflow not implemented. 42 leads unmapped (missing coordinates).
